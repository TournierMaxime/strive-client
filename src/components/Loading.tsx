import React from "react"

export default function Loading() {
  return (
    <div
      style={{
        display: "flex",
        flexDirection: "row",
        justifyContent: "center",
        marginTop: "2em",
      }}
    >
      Loading...
    </div>
  )
}
